# Fictional sanitized example

`SANITIZED_SAMPLE` demonstrates the report headings and directory organization.
It is not derived from a real scan and contains no machine identifiers.

Real scanner output may contain serial numbers, UUIDs, asset tags, NVRAM data,
boot configuration, storage or network identifiers, and EFI filenames. Never
publish an unreviewed `SCANS/` directory.